# Security

Use trusted local synthetic or authorized API contracts. V1 rejects remote refs, recursive schemas and unsupported versions; it is not a hostile-input process sandbox. Use JVM heap and CI time limits. Report files may contain contract excerpts; treat them with the same access controls as the inputs. Report vulnerabilities privately to the repository owner.
